%YAML 1.1
---
# Suricata configuration file
# default log directory
default-log-dir: /Users/goureshmadye/Downloads/suricata_logs

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types: [alert, http, dns, tls, files, flow, drop, stats]

  - fast:
      enabled: yes
      filename: fast.log
      append: yes
